import { Temperature } from '../../components/measurements/Temperature'

## Create a levain

Mix levain ingredients and leave in a non-reactive container in a warm area (<Temperature temp={20} />-<Temperature temp={25} />)
